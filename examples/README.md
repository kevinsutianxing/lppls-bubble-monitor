# Examples

The source paper is retained for methodology reference. Old risk charts were removed because they
visualized labels produced by the deprecated v2 classifier. Regenerate charts from a reviewed v3
artifact set with `generate_charts.py`.
