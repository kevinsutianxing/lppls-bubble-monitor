# Historical results removed

The v2 result files were removed because they were generated before boundary-saturation and
fit-validity gates. New runs are written to `artifacts/<run_id>/`, which is ignored by Git. Publish
only reviewed snapshots with their manifest and data hash.
