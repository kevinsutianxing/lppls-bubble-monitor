# Legacy API

The original root modules were removed from the production path because their seven-dimensional
calibration and permissive validity checks could classify boundary-saturated solutions as strong
bubble signals. Use the `lppls_monitor` package in `src/` instead.
